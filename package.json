{
  "name": "disc-golf-tracker",
  "version": "1.0.0",
  "description": "Track disc golf scores, view history, and compete with friends.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "dev": "npx wrangler pages dev",
    "deploy": "npx wrangler pages deploy --project-name disc-golf-tracker",
    "ingest": "node scripts/ingest-osm.mjs",
    "migrate": "npx wrangler d1 migrations apply disc-golf-tracker-db --remote",
    "migrate:local": "npx wrangler d1 migrations apply disc-golf-tracker-db --local",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  },
  "license": "MIT"
}
